void setup() {
  // letakkan kode pengaturan Anda di sini, untuk dijalankan satu kali:
  pinMode(13, OUTPUT);
}

void loop() {
  // letakkan kode utama Anda di sini untuk dijalankan berulang kali:
  digitalWrite(13, HIGH);
}
